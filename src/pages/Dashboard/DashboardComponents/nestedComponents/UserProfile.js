import React from 'react';
import { Container, Fade } from 'react-bootstrap';

export default () => (
  <Fade in appear>
    <Container>
      <h2>Ustawienia profilu</h2>
    </Container>
  </Fade>
);
